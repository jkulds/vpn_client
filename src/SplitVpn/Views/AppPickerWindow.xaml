<ui:FluentWindow x:Class="SplitVpn.Views.AppPickerWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Запущенные приложения" Height="600" Width="820"
                 WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Запущенные приложения">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="AppsList24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="16,4,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <ui:Button DockPanel.Dock="Right" Icon="{ui:SymbolIcon ArrowSync24}"
                           Content="Обновить" Click="OnRefresh" Margin="8,0,0,0" />
                <ui:TextBox x:Name="FilterBox" TextChanged="OnFilterChanged"
                            PlaceholderText="Фильтр по имени или пути"
                            Icon="{ui:SymbolIcon Filter24}" />
            </DockPanel>

            <ListBox Grid.Row="1" x:Name="AppList" SelectionMode="Extended"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold"
                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="1" Text="{Binding Path}" Style="{StaticResource Caption}"
                                       VerticalAlignment="Center" TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <TextBlock VerticalAlignment="Center" Margin="0,0,14,0" Style="{StaticResource Caption}"
                           Text="Можно выбрать несколько: Ctrl или Shift" />
                <ui:Button Content="Добавить" Appearance="Primary" IsDefault="True" Click="OnOk"
                           MinWidth="110" Margin="0,0,8,0" />
                <ui:Button Content="Отмена" IsCancel="True" MinWidth="110" Margin="0" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
