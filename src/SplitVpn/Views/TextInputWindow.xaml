<ui:FluentWindow x:Class="SplitVpn.Views.TextInputWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Height="460" Width="680"
                 WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="Bar">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Link24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="18,8,18,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="PromptText" Style="{StaticResource Caption}" Margin="0,0,0,10" />

            <ui:TextBox Grid.Row="1" x:Name="InputBox"
                        AcceptsReturn="True" TextWrapping="NoWrap"
                        VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                        VerticalContentAlignment="Top"
                        FontFamily="Cascadia Mono, Consolas" FontSize="12" />

            <!-- Второе поле показывается только когда его запросил вызывающий код (группа при импорте). -->
            <StackPanel Grid.Row="2" x:Name="SecondaryPanel" Visibility="Collapsed" Margin="0,12,0,0">
                <TextBlock x:Name="SecondaryLabel" Style="{StaticResource Caption}" Margin="0,0,0,5" />
                <ui:TextBox x:Name="SecondaryBox" Icon="{ui:SymbolIcon Folder24}" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <ui:Button Content="ОК" Appearance="Primary" IsDefault="True" Click="OnOk"
                           MinWidth="110" Margin="0,0,8,0" />
                <ui:Button Content="Отмена" IsCancel="True" MinWidth="110" Margin="0" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
